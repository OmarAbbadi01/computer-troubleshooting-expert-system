# التوثيق الشامل — نظام خبير لتشخيص مشاكل الحاسوب

---

## 1. مقدمة

نظام خبير يعمل من سطر الأوامر مكتوب بلغة بايثون خالصة، يهدف إلى تشخيص
فئات عامة من مشاكل الحاسوب. يسأل النظام المستخدم مجموعة صغيرة من الأسئلة
من نوع نعم/لا المتعلقة بالمشكلة المختارة، يجمع الإجابات كحقائق، ثم يستخدم
**محرك استدلال أمامي** (Forward Chaining) فوق **قاعدة معارف من القواعد
الشرطية إذا-فإن** لإنتاج تشخيصات محتملة مع شرح بسيط وتوصيات آمنة.

المشروع عبارة عن عرض أكاديمي لمفاهيم الأنظمة الخبيرة الكلاسيكية:
قاعدة المعارف، الذاكرة العاملة، قواعد إذا-فإن، الاستدلال الأمامي، الشرح
المنطقي، والفصل الواضح بين المعرفة (البيانات) ومنطق الاستدلال.

## 2. المتطلبات وطريقة التشغيل

**المتطلبات:**

- بايثون 3.9 أو أحدث.
- لا توجد أي مكتبات خارجية (المكتبة القياسية فقط).

**تشغيل البرنامج:**

```bash
python3 main.py
```

**تشغيل حالات الاختبار:**

```bash
python3 -m evaluation.run_evaluation
```

## 3. تحليل المشكلة

### 3.1 وصف المشكلة

قد يواجه مستخدم الحاسوب أعراضًا مثل: عدم التشغيل، شاشة فارغة، فشل الإقلاع،
بطء الأداء، ارتفاع الحرارة أو الانطفاء المفاجئ، فشل الشبكة، مشاكل التخزين،
مشاكل الذاكرة، أو عدم استجابة الأجهزة الطرفية.

يحاول النظام الاستدلال من الأعراض المرصودة إلى **أسباب محتملة عامة**.
لا يحاول تحديد المكوّن المعطوب بدقة في كل حالة؛ بل يعيد فئات تشخيصية عامة
مثل «مشكلة في الطاقة» أو «مشكلة في الشبكة»، ويمكن إرجاع أكثر من تشخيص واحد
عندما تدعم الأدلة ذلك.

### 3.2 المستخدم المستهدف

المستخدمون العامون أو الطلاب ذوو المعرفة الأساسية بالحاسوب. لذلك يعتمد
الاستبيان أسئلة نعم/لا بلغة بسيطة ويتجنّب المصطلحات التقنية.

### 3.3 الأهداف

- جمع أعراض المستخدم عبر مجموعة صغيرة من الأسئلة ذات الصلة.
- تمثيل الأعراض كحقائق داخلية.
- تطبيق قواعد خبير مخزّنة في قاعدة معارف منفصلة.
- استنتاج فئات المشاكل المحتملة بالاستدلال الأمامي.
- شرح المنطق وراء كل نتيجة بلغة بسيطة.
- تقديم توصيات بسيطة وآمنة.

### 3.4 النطاق

يغطي النظام أجهزة **المكتبي** و**المحمول** في المجالات التالية:
الطاقة، العرض، الإقلاع/نظام التشغيل، الأداء، ارتفاع الحرارة، الشبكات،
التخزين، الذاكرة، والأجهزة الطرفية.

### 3.5 ما ليس من أهداف المشروع

- تشخيص دقيق على مستوى المكوّنات.
- درجات الاحتمالية أو الثقة.
- إصلاحات أو إجراءات غير آمنة.

## 4. البنية المعمارية

مبدأ الفصل هو المطلب الأساسي في المشروع: المعرفة في البيانات، والآلية في
الكود.

```
المستخدم ← الاستبيان ← حقائق ← الذاكرة العاملة ← محرك الاستدلال الأمامي
                                                  ⇄ قاعدة المعارف (بيانات بايثون)
                                                    ↓
                                            تشخيص + شرح + توصية
```

| الملف | المسؤولية |
|-------|-----------|
| `main.py` | نقطة الدخول: نوع الجهاز، القائمة، الأسئلة، عرض النتيجة |
| `questionnaire.py` | ربط المشكلة المختارة بالأسئلة ذات الصلة وتحويل الإجابات إلى حقائق |
| `engine.py` | محرك الاستدلال الأمامي العام |
| `knowledge_base.py` | تحميل البيانات وتحويلها إلى كائنات |
| `models.py` | هياكل البيانات: `Rule`، `Question`، `MainProblem`، `FiredRule`، `WorkingMemory` |
| `data/rules.py` | 34 قاعدة إذا-فإن + بيانات التشخيصات |
| `data/questions.py` | 9 مشاكل رئيسية + 35 سؤالًا |
| `evaluation/run_evaluation.py` | تشغيل حالات الاختبار |
| `evaluation/cases.py` | حالات الاختبار الإحدى عشرة |

أفكار أساسية:

- **المعرفة بيانات لا كود.** القواعد الـ34 موجودة في `data/rules.py`
  والمحرك لا يفهم سوى مفاهيم عامة (شروط واستنتاجات) بلا أي منطق تشخيصي.
- **الاستدلال الأمامي.** يكرر المحرك العثور على القواعد المتحققة، يفعّلها
  بترتيب حتمي، ويضيف الحقائق المستنتجة إلى الذاكرة العاملة حتى الوصول إلى
  نقطة ثابتة.
- **الشرح.** كل قاعدة تحمل شرحًا مقروءًا والتشخيصات النهائية تحمل توصيات
  آمنة.

## 5. قاعدة المعرفة

قاعدة المعرفة مخزّنة كمتغيرات بايثون في `data/rules.py` ومنفصلة تمامًا
عن محرك الاستدلال؛ المحرك يعرف فقط كيف يفكر في القواعد التي تُعطى له.

### 5.1 مصادر المعرفة التشخيصية

- أدلة إصلاح أجهزة الحاسوب الشخصي التي تربط الأعراض بالأسباب.
- وثائق دعم أنظمة التشغيل لإصلاح بدء التشغيل والوضع الآمن واستكشاف أخطاء
  الشبكة.
- وثائق أساسية للعتاد لفحص سلامة الذاكرة والقرص واستكشاف الأجهزة الطرفية.

### 5.2 تصنيفات الحقائق

| الفئة | أمثلة على الحقائق |
|-------|-------------------|
| الطاقة | `power_led_on`، `fans_not_running`، `power_cable_loose`، `outlet_power_fails` |
| العرض | `screen_blank`، `post_screen_blank`، `external_monitor_works` |
| الإقلاع / نظام التشغيل | `os_loading_screen_visible`، `bsod_occurs`، `post_screen_visible` |
| الأداء | `os_loads_slowly`، `programs_launch_slowly`، `many_startup_programs` |
| ارتفاع الحرارة | `computer_hot`، `shuts_down_unexpectedly`، `vents_blocked` |
| الشبكة | `wifi_connected`، `router_reachable`، `internet_unreachable` |
| التخزين | `disk_nearly_full`، `disk_activity_high`، `disk_errors_reported` |
| الذاكرة | `ram_usage_high`، `system_freezes`، `ram_sticks_missing` |
| الملحقات | `device_not_detected`، `device_fails_any_port`، `driver_issue` |

### 5.3 الحقائق الوسيطة

حقائق يُستنتج بين الإدخال والتشخيص لإظهار الاستدلال المتسلسل الحقيقي:

- `power_delivery_issue`، `internal_power_fault`
- `display_path_fault`، `backlight_or_panel_fault`
- `os_boot_reached`، `os_boot_failed`
- `system_slow_indicators`، `startup_overload`
- `cooling_problem`
- `local_connectivity_only`
- `storage_pressure`، `memory_pressure`

### 5.4 التشخيصات النهائية

| الحقيقة | الاسم |
|---------|-------|
| `diagnosis_power` | مشكلة في الطاقة |
| `diagnosis_display` | مشكلة في الشاشة |
| `diagnosis_boot` | مشكلة في الإقلاع/نظام التشغيل |
| `diagnosis_performance` | مشكلة في الأداء |
| `diagnosis_overheating` | مشكلة في ارتفاع الحرارة |
| `diagnosis_network` | مشكلة في الشبكة |
| `diagnosis_storage` | مشكلة في التخزين |
| `diagnosis_memory` | مشكلة في الذاكرة |
| `diagnosis_peripheral` | مشكلة في الملحق/الجهاز الطرفي |

أي حقيقة تبدأ بالبادئة `diagnosis_` يُعاملها النظام كتشخيص نهائي.

### 5.5 القواعد (34 قاعدة)

الشروط كلها شرط **و** (يجب أن تتوفر جميعًا). للمنطق «أو» تُكتب قواعد منفصلة
(مثل P1 وP2 وكلتاهما تستنتج `power_delivery_issue`).

| القاعدة | الشروط ← الاستنتاجات |
|---------|-----------------------|
| P1 | `power_cable_loose` → `power_delivery_issue` |
| P2 | `outlet_power_fails` → `power_delivery_issue` |
| P3 | `power_delivery_issue` + `power_led_off` → `diagnosis_power` |
| P4 | `power_led_off` + `fans_not_running` + `power_cable_ok` + `outlet_power_ok` → `internal_power_fault` |
| P5 | `internal_power_fault` → `diagnosis_power` |
| D1 | `computer_running` + `screen_blank` + `post_screen_blank` → `display_path_fault` |
| D2 | `display_path_fault` → `diagnosis_display` |
| D3 | `external_monitor_works` + `screen_blank` → `backlight_or_panel_fault` |
| D4 | `backlight_or_panel_fault` → `diagnosis_display` |
| B1 | `os_loading_screen_visible` → `os_boot_reached` |
| B2 | `os_boot_reached` + `bsod_occurs` → `diagnosis_boot` |
| B3 | `post_screen_visible` + `os_loading_screen_absent` → `os_boot_failed` |
| B4 | `os_boot_failed` → `diagnosis_boot` |
| PERF1 | `os_loads_slowly` + `programs_launch_slowly` → `system_slow_indicators` |
| PERF2 | `system_slow_indicators` + `disk_nearly_full` → `diagnosis_performance` |
| PERF3 | `system_slow_indicators` + `many_startup_programs` → `startup_overload` |
| PERF4 | `startup_overload` → `diagnosis_performance` |
| O1 | `computer_running` + `computer_hot` + `fans_not_running` → `cooling_problem` |
| O2 | `computer_hot` + `vents_blocked` → `cooling_problem` |
| O3 | `cooling_problem` + `shuts_down_unexpectedly` → `diagnosis_overheating` |
| O4 | `computer_hot` + `fan_unusually_noisy` + `shuts_down_unexpectedly` → `diagnosis_overheating` |
| NET1 | `wifi_connected` + `router_reachable` + `internet_unreachable` → `local_connectivity_only` |
| NET2 | `local_connectivity_only` → `diagnosis_network` |
| NET3 | `wifi_no_networks` + `other_devices_offline` → `diagnosis_network` |
| NET4 | `ip_address_missing` + `router_reachable` + `wifi_connected` → `diagnosis_network` |
| S1 | `disk_nearly_full` + `disk_activity_high` → `storage_pressure` |
| S2 | `storage_pressure` → `diagnosis_storage` |
| S3 | `disk_errors_reported` + `files_missing_corrupt` → `diagnosis_storage` |
| M1 | `ram_usage_high` + `system_freezes` → `memory_pressure` |
| M2 | `memory_pressure` → `diagnosis_memory` |
| M3 | `ram_sticks_missing` + `frequent_app_crashes` → `diagnosis_memory` |
| PE1 | `device_not_detected` + `device_fails_any_port` + `device_fails_other_computer` → `diagnosis_peripheral` |
| PE2 | `device_not_detected` + `device_works_in_another_port` → `diagnosis_peripheral` |
| PE3 | `device_detected` + `driver_issue` → `diagnosis_peripheral` |

### 5.6 تنظيم القواعد

- **التوزيع:** نحو 4–5 قواعد لكل مجال، بإجمالي 34 قاعدة.
- **التسلسل:** نحو نصف القواعد يستنتج حقائق وسيطة والباقي يحوّل الأدلة إلى
  تشخيصات نهائية. عدة تشخيصات تتطلب خطوتين وواحد يتطلب ثلاث خطوات.
- **ترتيب التفعيل:** تُفعَّل القواعد بترتيب حتمي حسب معرّف القاعدة.
- **استنتاجات متعددة:** القواعد غير متناقضة؛ يمكن لقواعد مختلفة الوصول إلى
  التشخيص نفسه من أدلة مختلفة، وقد تُفعَّل عدة سلاسل مستقلة في جلسة واحدة.

## 6. تصميم آلية الاستدلال الأمامي

### 6.1 لماذا الاستدلال الأمامي؟

يبدأ المستخدم من **أعراض معلومة** (حقائق) وعلى النظام أن يستنتاج **أي
الاستنتاجات مدعومة**. هذا تفكير موجّه بالبيانات من الحقائق نحو الهدف.

### 6.2 الذاكرة العاملة

`WorkingMemory` (في `engine.py`) تخزّن:

- `facts` — مجموعة الحقائق الحالية (من الإجابات ومن القواعد المُفعَّلة).
- `fired_rules` — سجل مرتب لكل تفعيل قاعدة، يُستخدم لبناء الشرح.

### 6.3 مطابقة القواعد

القاعدة *قابلة للتطبيق* عندما تكون كل حقيقة في `conditions` موجودة في
الذاكرة العاملة الحالية:

```python
applicable = [r for r in rules if all(c in memory.facts for c in r.conditions)]
```

### 6.4 ترتيب التفعيل (حتمي)

تُفرز القواعد القابلة للتطبيق حسب معرّف القاعدة ليكون السلوك متطابقًا في
كل تشغيل:

```python
applicable.sort(key=lambda r: r.id)
```

### 6.5 الإنهاء عند النقطة الثابتة

يكرر المحرك دورة المطابقة/التفعيل حتى لا تنتج أي قاعدة حقيقة جديدة. لأن كل
قاعدة تُفعَّل مرة واحدة على الأكثر لنفس الحقائق، والحقائق تُضاف فقط ولا
تُحذف، فتنتهي العملية دائمًا عند نقطة ثابتة.

### 6.6 مثال على سلسلة استدلال

```
الحقائق الأولية:
  computer_running, computer_hot, fans_not_running, shuts_down_unexpectedly

الخطوة 1 — القاعدة O1: إذا computer_running و computer_hot و fans_not_running
                      فإن cooling_problem
الخطوة 2 — القاعدة O3: إذا cooling_problem و shuts_down_unexpectedly
                      فإن diagnosis_overheating

التشخيص: مشكلة في ارتفاع الحرارة
```

### 6.7 تشخيصات متعددة

لا يفرض المحرك إجابة واحدة؛ كل قاعدة تحققت شروطها تُفعَّل، فقد تظهر عدة
تشخيصات في الذاكرة العاملة معًا.

### 6.8 الأدلة غير الكافية

إذا لم تصل أي قاعدة إلى حقيقة `diagnosis_*`، يعرض النظام ببساطة أنه «لا
يمكن استخلاص أي نتيجة من الإجابات المعطاة».

### 6.9 تتبع الشرح

كل قاعدة مُفعَّلة تُسجَّل مع الحقائق التي أضافتها. تعرض طبقة العرض الشرح
المقروء المرفق بالقاعدة التي أنتجت التشخيص.

## 7. الاختبار والتقييم

حُدّدت حالات التقييم **بعد** تثبيت الاستبيان والحقائق والقواعد وسلوك
الاستدلال. نُفّذت الحالات عبر الاستبيان ومحرك الاستدلال الحقيقيين،
وجميعها نجحت. التطبيق نفسه لا يحتوي أي منطق خاص بسيناريوهات الاختبار.

### 7.1 الحالات الإحدى عشرة

| # | الحالة | المشكلة | التشخيص المتوقع | النتيجة |
|---|--------|---------|------------------|---------|
| 1 | الطاقة | لا يعمل الحاسوب | مشكلة في الطاقة | PASS |
| 2 | الشاشة | يعمل الحاسوب لكن الشاشة فارغة | مشكلة في الشاشة | PASS |
| 3 | الإقلاع | لا يُقلع الحاسوب بشكل صحيح | مشكلة في الإقلاع/نظام التشغيل | PASS |
| 4 | الشبكة | الإنترنت/الشبكة لا تعمل | مشكلة في الشبكة | PASS |
| 5 | ارتفاع الحرارة | يسخن الحاسوب أو ينطفئ فجأة | مشكلة في ارتفاع الحرارة | PASS |
| 6 | الأداء | الحاسوب بطيء | مشكلة في الأداء | PASS |
| 7 | التخزين | مشكلة في التخزين | مشكلة في التخزين | PASS |
| 8 | الذاكرة | مشكلة في الذاكرة | مشكلة في الذاكرة | PASS |
| 9 | الملحقات | الملحق/الجهاز الطرفي لا يعمل | مشكلة في الملحق/الجهاز الطرفي | PASS |
| 10 | تشخيصات متعددة | الحاسوب بطيء | مشكلة في الأداء + مشكلة في التخزين | PASS |
| 11 | أدلة غير كافية | يسخن الحاسوب أو ينطفئ فجأة | لا تشخيص نهائي | PASS |

### 7.2 ملاحظات على التغطية

- **تشخيصات متعددة:** الحالة 10 — سلسلتان مستقلتان في جلسة واحدة.
- **تسلسل متعدد الخطوات:** الحالات 1–8 و10 تُظهر سلسلة من خطوتين على الأقل؛
  الحالة 6 تُظهر سلسلة من ثلاث خطوات.
- **أدلة غير كافية:** الحالة 11 — استُنتجت مشكلة تبريد لكن لا قاعدة تشخيص
  تحققت.
- **تكيّف المحمول:** سؤال الشاشة الخارجية يُطرح للمحمول فقط.

## 8. الأسئلة الشائعة

### س1: ما هو النظام الخبير؟

برنامج يحاكي طريقة اتخاذ القرار عند الخبير البشري. يخزّن المعرفة البشرية
على شكل قواعد، ويستخدم محرك استدلال للتفكير بناءً عليها.

### س2: ما هي الحقيقة (Fact)؟

قطعة صغيرة من المعلومات تُخزَّن في الذاكرة العاملة. تكون إمّا مقدَّمة من
المستخدم أو مستنتَجة من قاعدة.

### س3: أين توجد قاعدة المعرفة؟

في `data/rules.py`. تحتوي جميع القواعد الـ34 والتشخيصات والشروح والتوصيات.

### س4: ما هي القاعدة؟

جملة «إذا-فإن». لها شروط (قائمة حقائق يجب أن تكون موجودة) واستنتاجات
(حقائق تصبح صحيحة عند تحقق الشروط).

### س5: أين يوجد محرك الاستدلال؟

في `engine.py` — داخل كلاس `InferenceEngine`. وهو محرك عام تمامًا لا يحتوي
أي منطق لتشخيص الأعطال.

### س6: لماذا الاستدلال الأمامي؟

لأننا نبدأ من حقائق معلومة (أعراض المستخدم) ونتقدّم نحو نتيجة. هذا تفكير
موجّه بالبيانات.

### س7: كيف تفعّل حقيقة مستنتجة قاعدة أخرى؟

بعد تفعيل قاعدة تُضاف استنتاجاتها إلى الذاكرة العاملة، وفي الدورة التالية
يمكن لحقيقة جديدة أن تجعل شروط قاعدة أخرى متحققة.

### س8: لماذا فصل قاعدة المعرفة عن منطق البرنامج؟

لكي تكون المعرفة والآلية مستقلتين: تعديل القواعد لا يتطلب لمس الكود.

### س9: كيف يشرح النظام نتيجته؟

تحمل كل قاعدة حقل `explanation` بلغة بسيطة. عند إنتاج تشخيص يعرض البرنامج
شرح القاعدة (أو القواعد) التي وصلت إليه.

### س10: لماذا أسئلة نعم/لا فقط؟

لإبقاء الاستبيان بسيطًا والحقائق غير ملتبسة.

### س11: لماذا يعطي النظام أحيانًا أكثر من تشخيص؟

لأن الاستدلال الأمامي يفعّل كل قاعدة قابلة للتفعيل؛ إذا دعمت الأدلة عدة
استنتاجات تُعرض جميعها.

### س12: ماذا يحدث عندما لا تكفي الأدلة؟

يقول البرنامج إنه لا يمكن استخلاص أي استنتاج من الإجابات المعطاة.

### س13: لماذا بايثون مع المكتبة القياسية فقط؟

لأن المطلوب بايثون خالص مع منع الأطر والاعتماديات غير الضرورية.

## 9. الحدود والعمل المستقبلي

**الحدود:**

- التشخيصات عامة وليست على مستوى المكوّنات.
- أسئلة ثابتة وبصيغة نعم/لا فقط.
- مجموعة قواعد ثابتة غير قابلة للتعلم.
- لا احتمالات أو درجات ثقة.

**العمل المستقبلي:**

- المزيد من القواعد وتشخيصات أدق.
- اختيار أسئلة ديناميكي بناءً على الإجابات السابقة.
- احتمالات أو معاملات ثقة.
- واجهة رسومية صغيرة.

## 10. هيكل التقرير الأكاديمي

قالب للتقرير الجامعي النهائي:

1. **المقدمة** — بيان المشكلة والهدف، ونظرة على الأنظمة الخبيرة.
2. **تحليل المشكلة** — الأعراض المغطاة، المستخدم المستهدف، الأهداف، النطاق.
3. **اكتساب المعرفة** — المصادر وكيف تُرجمت الأعراض إلى حقائق وقواعد.
4. **قاعدة المعرفة** — القواعد الـ34، مفردات الحقائق، الحقائق الوسيطة،
   التشخيصات النهائية.
5. **تمثيل المعرفة** — الحقائق، قواعد إذا-فإن مع الشروح.
6. **بنية النظام** — سطر الأوامر، الاستبيان، الذاكرة العاملة، قاعدة المعرفة،
   محرك الاستدلال.
7. **آلية الاستدلال الأمامي** — المطابقة، ترتيب التفعيل، الإنهاء عند النقطة
   الثابتة.
8. **التنفيذ** — وصف لكل وحدة ولماذا كفت بايثون الخالصة.
9. **واجهة المستخدم** — قائمة سطر الأوامر، أسئلة نعم/لا، عرض النتيجة.
10. **الاختبار والتقييم** — الحالات الـ11، المتوقع مقابل الفعلي.
11. **النتائج** — نجاح جميع الحالات، دعم التشخيصات المتعددة.
12. **نقاط القوة والحدود**.
13. **التوصيات / التحسينات المستقبلية**.
14. **المراجع**.
15. **لقطات الشاشة** — القائمة الرئيسية، تدفق الأسئلة، التشخيص + الشرح.
